## Add password reset to login page ##

Step 1: Add password_reset url in urls.py file using auth_views
    auth_views.PasswordResetView.as_view(template_name='users/password_reset.html')

Step 2: Create template similar to login.html with necessary change.
It will have email where it sends link to reset password with instruction

Step 3: Add url password_reset/done in urls.py

Step 4: Create template, similar to logout having simple alert message.

** test password_reset/ on browser. after inserting email and submit button,
 it will throw an error. Its looking for password_reset_confirm route.
 error shows the required inputs expected, uidb64 and token to send email. **

Step 5: Add url path for password_reset_confirm/ with view
    url will be 'password-reset-confirm/<uidb64>/<token>/'

Step 6: Create template for password_reset_confirm.

Step 7: Setup gmail for sending emails using google app password, that allows 
password for less secure pages. 

Step 8: Add few variables in settings.py that is required to send emails
    EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
    EMAIL_HOST = 'smtp.gmail.com'
    EMAIL_PORT = 587
    EMAIL_USE_TLS = True
    EMAIL_HOST_USER = os.getenv('EMAIL_USER')
    EMAIL_HOST_PASSWORD = os.getenv('EMAIL_PASS')

Step 9: Try to send mail from django shell to verify success 
    python .\manage.py shell
    >>> from django.core.mail import send_mail
    >>> send_mail(
    ...     subject="Test Email",
    ...     message="This is a test email from Django.",
    ...     from_email="your_mail@gmail.com",
    ...     recipient_list=["your_mail@gmail.com"],
    ...     fail_silently=False,
    ... )
    1
    >>>

** once tested successfully, verify same from webapp.
link will be sent to reset password form 
click on it and reset new password and submit.
Next, app will route to password_reset_complete**

Step 10: Lastly, add url for password_reset_complete/  
create template with a message and add url to log in again.

Step 11: password_reset link must be in login page 
add a link to password_reset in login.html as forget password?

** test complete password_reset process to make sure all pages are working **